<x-app-layout>
    <x-slot name="header">
        <h1 class="h3 mb-0">{{ __('Dashboard') }}</h1>
    </x-slot>

    <div class="card">
        <div class="card-body">
            <p class="mb-0">{{ __("You're logged in!") }}</p>
        </div>
    </div>
</x-app-layout>
